# ============================================================
# 环境变量示例（复制为 .env 后按需填写；.env 已被 .gitignore 排除，不会进 Git）
# 服务启动时会自动加载项目根 .env（server/utils/load-env.js），
# 已存在的系统环境变量优先（.env 不覆盖）。
# ============================================================

# ---- 服务 ----
# 服务端口（默认 3000）
# PORT=3000

# JWT 签名密钥：生产环境必须设置为足够长的随机字符串（严禁使用开发默认值）
# 生成示例：node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# JWT_SECRET=

# 登录/注册接口限流：每 IP 每 10 分钟最大次数（默认 20；本地自动化测试可调大）
# AUTH_RATE_LIMIT_MAX=20

# ---- Supabase（可选；未配置时自动回退 JSON 文件存储） ----
# 项目 URL（Supabase Dashboard → Settings → API）
# SUPABASE_URL=https://<your-project-ref>.supabase.co

# 服务端 key：请使用 service_role key（Supabase Dashboard → Settings → API → service_role，形如 JWT 长字符串）。
# ⚠ 安全要求：service_role 绕过 RLS，只能配置在后端环境，严禁放入前端代码/仓库/日志。
# SUPABASE_SERVICE_KEY=<your-service-role-key-here>
